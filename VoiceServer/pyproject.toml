[project]
name = "pai-voice-server"
version = "2.0.0"
description = "ElevenLabs TTS proxy server for PAI on Windows"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]
